# マルチエージェントシステム - クイックスタート

## 🎯 概要

FAQ_Geminiが他のエージェント（IoT、ブラウザ）と協調動作できるようになりました！

```
┌─────────────────────────────────────────────────────────┐
│           マルチエージェントシステム                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   FAQ Agent  │  │  IoT Agent   │  │Browser Agent │ │
│  │  (このRepo)   │  │              │  │              │ │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤ │
│  │ナレッジ      │  │デバイス制御   │  │Web検索       │ │
│  │ベース検索    │  │センサー監視   │  │ブラウザ操作  │ │
│  │会話分析      │  │状態確認      │  │情報抽出      │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│         ↑                ↑                 ↑           │
│         └────────────────┴─────────────────┘           │
│              エージェント間通信可能                      │
└─────────────────────────────────────────────────────────┘
```

## 🚀 クイックスタート

### 1. エージェント情報を取得

```python
from agent_registry import get_agent_info

# IoTエージェントの情報を取得
agent = get_agent_info("iot")
print(f"名前: {agent.display_name}")
print(f"接続先: {agent.base_urls[0]}")
```

### 2. 最適なエージェントを選択

```python
from agent_registry import get_optimal_agent_for_task

# タスクに応じて最適なエージェントを自動選択
task = "部屋の温度を25度に設定したい"
agent = get_optimal_agent_for_task(task)
# → IoT エージェント
```

### 3. REST APIで利用

```bash
# 全エージェント一覧
curl http://localhost:5000/agents

# 最適なエージェントを提案
curl -X POST http://localhost:5000/agents/suggest \
  -H "Content-Type: application/json" \
  -d '{"task": "Webで天気を調べて"}'
```

## 📚 ドキュメント

| ファイル | 説明 |
|---------|------|
| [AGENTS_INFO.md](AGENTS_INFO.md) | 各エージェントの詳細仕様とAPI |
| [USAGE_EXAMPLES.md](USAGE_EXAMPLES.md) | 実装パターンと使用例 |
| [IMPLEMENTATION_SUMMARY.md](IMPLEMENTATION_SUMMARY.md) | 実装の完全なサマリー |

## 🤝 エージェント一覧

### FAQエージェント（家庭内エージェント）
- **役割**: ナレッジベース検索、家電の使い方
- **接続先**: `http://localhost:5000`
- **主要API**: `/rag_answer`, `/analyze_conversation`

### IoTエージェント
- **役割**: IoTデバイス制御、センサー監視
- **接続先**: `https://iot-agent.project-kk.com`
- **主要API**: `/api/chat`, `/api/devices`
- **リポジトリ**: [IoT-Agent](https://github.com/kota-kawa/IoT-Agent)

### ブラウザエージェント
- **役割**: Web検索、ブラウザ自動化
- **接続先**: `http://localhost:5005`
- **主要API**: `/api/chat`, `/api/stream`
- **リポジトリ**: [web_agent02](https://github.com/kota-kawa/web_agent02)

## 💡 使用例

### エージェント間通信パターン

```python
import requests
from agent_registry import get_optimal_agent_for_task

def handle_request(user_message):
    # 1. 最適なエージェントを選択
    agent = get_optimal_agent_for_task(user_message)
    
    # 2. そのエージェントに転送
    if agent.agent_type.value == "iot":
        response = requests.post(
            f"{agent.base_urls[0]}/api/chat",
            json={"messages": [{"role": "user", "content": user_message}]}
        )
        return response.json()["reply"]
```

## ✅ テスト

```bash
python test_agent_registry.py
```

期待される出力:
```
============================================================
FAQ_Gemini エージェントレジストリテスト
============================================================

✓ モジュールインポート成功
✓ エージェント情報取得
✓ 別名によるエージェント取得
✓ タスクからのエージェント選択
✓ 全エージェント一覧取得
✓ 能力によるエージェント検索
✓ エンドポイント情報取得

全てのテストが成功しました! ✓
```

## 🔗 参考リンク

- [Multi-Agent-Platform](https://github.com/kota-kawa/Multi-Agent-Platform) - マルチエージェントオーケストレーター
- [IoT-Agent](https://github.com/kota-kawa/IoT-Agent) - IoTデバイス制御
- [web_agent02](https://github.com/kota-kawa/web_agent02) - ブラウザ自動化

## 📝 ライセンス

このプロジェクトは元のFAQ_Geminiリポジトリと同じライセンスです。
